<h1><%= @house.title %></h1>

<p><%= @house.description %></p>

<ul>
  <li><%= link_to "Edit", edit_house_path(@house) %></li>
  <li><%= link_to "Destroy", house_path(@house), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<h2>Comments</h2>
<%= render @house.comments %>


<h2>Add a comment:</h2>
<%= render 'comments/form' %>